# دليل سريع للرفع على Netlify 🚀

## الطريقة الأسهل (سحب وإسقاط)

### الخطوات:

1. **سجل دخول إلى Netlify**
   - اذهب إلى: https://app.netlify.com/
   - سجل دخول أو أنشئ حساب جديد (مجاني)

2. **ارفع المشروع**
   - في الصفحة الرئيسية، ستجد منطقة "Want to deploy a new site without connecting to Git?"
   - اسحب مجلد المشروع بالكامل وأسقطه في المربع
   - انتظر حتى يكتمل الرفع (عادة دقيقة واحدة)

3. **أضف مفتاح API**
   - بعد الرفع، اذهب إلى: Site settings → Environment variables
   - اضغط "Add a variable"
   - الاسم: `MISTRAL_API_KEY`
   - القيمة: مفتاح API الخاص بك من Mistral
   - اضغط "Save"

4. **أعد نشر الموقع**
   - اذهب إلى: Deploys
   - اضغط "Trigger deploy" → "Deploy site"

5. **جاهز! 🎉**
   - ستجد رابط موقعك أعلى الصفحة
   - مثال: `https://your-site-name.netlify.app`

## الطريقة الثانية (عبر Git)

### 1. ارفع المشروع إلى GitHub:

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Deploy to Netlify"

# أنشئ مستودع جديد على GitHub أولاً، ثم:
git remote add origin https://github.com/YOUR_USERNAME/arabic-ocr.git
git branch -M main
git push -u origin main
```

### 2. اربط Netlify بـ GitHub:

1. في Netlify، اضغط "New site from Git"
2. اختر GitHub
3. اختر المستودع
4. الإعدادات:
   - Build command: `echo 'Build completed'`
   - Publish directory: `static`
5. اضغط "Deploy site"

### 3. أضف متغيرات البيئة:
(نفس الخطوة 3 من الطريقة الأولى)

## اختبار الموقع

بعد النشر، اختبر:
- ✅ الصفحة الرئيسية: `/`
- ✅ التطبيق: `/index.html`
- ✅ رفع صورة أو PDF
- ✅ استخراج النص

## حل المشاكل الشائعة

### المشكلة: "Error: Function not found"
**الحل**: تأكد من وجود مجلد `netlify/functions/` في المشروع

### المشكلة: "API key not found"
**الحل**: تأكد من إضافة `MISTRAL_API_KEY` في Environment variables

### المشكلة: الموقع لا يعمل
**الحل**: 
1. تحقق من السجلات في: Deploys → (اختر آخر deploy) → Deploy log
2. تأكد من وجود ملف `netlify.toml` في جذر المشروع

## ملاحظات مهمة ⚠️

- الخطة المجانية توفر: 100 GB نقل بيانات/شهر
- Functions محدودة بـ 10 ثوانٍ في الخطة المجانية
- لا تشارك مفتاح API مع أحد

## الدعم

إذا واجهت أي مشكلة:
1. راجع السجلات في Netlify Dashboard
2. تحقق من [وثائق Netlify](https://docs.netlify.com/)
3. راجع ملف `DEPLOYMENT.md` للتفاصيل الكاملة

---

**تم! الآن موقعك مباشر على الإنترنت 🌐**
